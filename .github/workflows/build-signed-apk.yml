name: Build Signed APK

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Grant execute permission for gradlew
      run: chmod +x android-apk/gradlew

    - name: Build Release APK
      run: |
        cd android-apk
        ./gradlew assembleRelease

    - name: Sign APK
      if: ${{ secrets.ANDROID_KEYSTORE }}
      uses: r0adkll/sign-android-release@v1
      with:
        releaseDirectory: android-apk/app/build/outputs/apk/release
        signingKeyBase64: ${{ secrets.ANDROID_KEYSTORE }}
        alias: ${{ secrets.ANDROID_KEY_ALIAS }}
        keyStorePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
      env:
        BUILD_TOOLS_VERSION: "34.0.0"

    - name: Rename signed APK
      if: ${{ secrets.ANDROID_KEYSTORE }}
      run: |
        mv android-apk/app/build/outputs/apk/release/*-signed.apk android-apk/app/build/outputs/apk/release/stardew-ids.apk

    - name: Upload Signed APK
      uses: actions/upload-artifact@v4
      with:
        name: stardew-ids-signed
        path: |
          android-apk/app/build/outputs/apk/release/stardew-ids.apk
          android-apk/app/build/outputs/apk/release/app-release-unsigned.apk

    - name: Create Release
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v1
      with:
        files: |
          android-apk/app/build/outputs/apk/release/stardew-ids.apk
          android-apk/app/build/outputs/apk/release/app-release-unsigned.apk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}